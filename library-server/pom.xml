<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ==================== 父 POM 坐标 ==================== -->
    <groupId>com.library</groupId>
    <artifactId>library-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>LibrarySystem</name>
    <description>图书馆智能管理系统 — 后端 Maven 多模块项目</description>

    <!-- ==================== 子模块声明 ==================== -->
    <modules>
        <module>library-common</module>
        <module>library-ai</module>
        <module>library-core</module>
        <module>library-knowledge-graph</module>
        <module>library-acquisition</module>
        <module>library-security</module>
        <module>library-bootstrap</module>
    </modules>

    <!-- ==================== Spring Boot 父依赖 ==================== -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <!-- ==================== 全局版本管理 ==================== -->
    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 核心框架 -->
        <spring-boot.version>3.5.0</spring-boot.version>

        <!-- 数据库 & ORM -->
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <mysql-connector.version>8.0.33</mysql-connector.version>

        <!-- 搜索 & 图数据库 -->
        <elasticsearch.version>8.11.0</elasticsearch.version>
        <!-- neo4j-java-driver 版本由 Spring Boot 3.5 dependencyManagement 管理（兼容 Spring Data Neo4j 7.4），
             父 POM 不再强制 5.17.0——该版本缺 Notification.classification() 致 NoSuchMethodError。
             driver 5.x 向下兼容 docker-compose 的 Neo4j server 5.17.0。 -->

        <!-- 缓存 & 分布式 -->
        <redisson.version>3.25.0</redisson.version>

        <!-- API 文档 -->
        <springdoc.version>2.6.0</springdoc.version>
        <!-- 注：SpringDoc 版本已升级以兼容 Spring Boot 3.5.x -->

        <!-- NLP & 数学库 -->
        <!-- 注：HanLP 2.x 基于 PyTorch，不以传统 JAR 发布至 Maven Central；此处使用 1.x portable 版本满足本地 NLP 需求 -->
        <hanlp.version>portable-1.8.5</hanlp.version>
        <commons-math3.version>3.6.1</commons-math3.version>

        <!-- 工具库 -->
        <hutool.version>5.8.25</hutool.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>

        <!-- 数据库迁移 -->
        <flyway.version>9.22.3</flyway.version>

        <!-- JWT -->
        <jjwt.version>0.12.5</jjwt.version>

        <!-- 插件版本 -->
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.1.2</maven-surefire-plugin.version>

        <!-- 测试基础设施（阶段10 集成测试与加固） -->
        <!-- 集成测试改用 docker-compose 真实中间件（Testcontainers 与 Docker Desktop 29 不兼容，已移除） -->
        <awaitility.version>4.2.2</awaitility.version>
        <org-jacoco.version>0.8.12</org-jacoco.version>
    </properties>

    <!-- ==================== 依赖管理（统一版本，子模块按需引入） ==================== -->
    <dependencyManagement>
        <dependencies>
            <!-- ===== 本项目的子模块间依赖 ===== -->
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-ai</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-knowledge-graph</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-acquisition</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.library</groupId>
                <artifactId>library-security</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- ===== MyBatis-Plus ===== -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- ===== Elasticsearch ===== -->
            <dependency>
                <groupId>co.elastic.clients</groupId>
                <artifactId>elasticsearch-java</artifactId>
                <version>${elasticsearch.version}</version>
            </dependency>

            <!-- ===== Neo4j ===== -->
            <!-- neo4j-java-driver 版本由 Spring Boot 3.5 dependencyManagement 管理，兼容 Spring Data Neo4j 7.4
                 （含 Notification.classification()）。driver 5.x 兼容 docker-compose 的 Neo4j server 5.17.0。 -->

            <!-- ===== Redisson（分布式锁 / 排队） ===== -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- ===== SpringDoc OpenAPI ===== -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ===== HanLP 中文 NLP ===== -->
            <dependency>
                <groupId>com.hankcs</groupId>
                <artifactId>hanlp</artifactId>
                <version>${hanlp.version}</version>
            </dependency>

            <!-- ===== Apache Commons Math（OLS 回归） ===== -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-math3</artifactId>
                <version>${commons-math3.version}</version>
            </dependency>

            <!-- ===== Hutool 工具集 ===== -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- ===== MapStruct（对象映射） ===== -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- ===== Flyway（数据库迁移） ===== -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-mysql</artifactId>
                <version>${flyway.version}</version>
            </dependency>

            <!-- ===== JWT（jjwt） ===== -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- ==================== 公共依赖（所有模块继承） ==================== -->
    <dependencies>
        <!-- 编译时 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 测试基础设施（阶段10，全局 test scope，所有模块可写集成测试） -->
        <!-- Awaitility：异步事件（MQ 消费）等待断言。中间件由 docker-compose 预启动，连接用 application.yml 默认 localhost。 -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- ==================== 构建配置 ==================== -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <!-- 默认排除集成测试（需 docker-compose 中间件），日常 mvn test 仅跑单元测试；
                             mvn test -Pintegration 时清空 excludes 跑全量（含集成测试） -->
                        <excludes>
                            <exclude>**/integration/**</exclude>
                            <exclude>**/LibraryApplicationTests.java</exclude>
                        </excludes>
                        <forkedProcessTimeoutInSeconds>1800</forkedProcessTimeoutInSeconds>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>3.4.1</version>
                    <executions>
                        <execution>
                            <id>enforce-versions</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requireJavaVersion>
                                        <version>[17,)</version>
                                    </requireJavaVersion>
                                    <requireMavenVersion>
                                        <version>[3.9,)</version>
                                    </requireMavenVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <!-- JaCoCo 覆盖率（阶段10，对照架构文档 §11.4 目标） -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${org-jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <!-- 排除非业务逻辑：配置/实体/DTO/Mapper 接口/启动类 -->
                        <excludes>
                            <exclude>**/config/**</exclude>
                            <exclude>**/entity/**</exclude>
                            <exclude>**/dto/**</exclude>
                            <exclude>**/mapper/**</exclude>
                            <exclude>**/*Application.class</exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- 全局激活 JaCoCo prepare-agent，所有子模块继承生成覆盖率数据 -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- 集成测试 profile：mvn test -Pintegration 跑全量（含 docker-compose 集成测试）。
             日常 mvn test（无此 profile）仅跑单元测试，保护现有 368 项不被慢测试拖累。
             前置：docker-compose up -d 启动 5 中间件（MySQL/Redis/ES+IK/Neo4j/RabbitMQ）。 -->
        <profile>
            <id>integration</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!-- 清空默认 excludes，跑 integration 包 + LibraryApplicationTests -->
                            <excludes combine.self="override"/>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
