# =============================================================================
# 图书馆智能管理系统 — 测试环境配置
# =============================================================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_db_test?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/test-data

logging:
  level:
    com.library: DEBUG
