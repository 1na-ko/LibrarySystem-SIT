-- =============================================================================
-- 图书馆智能管理系统 — Docker MySQL 初始化脚本
-- 此文件在 MySQL 容器首次启动时自动执行（仅在数据卷不存在时）
-- 业务表 DDL 由 Flyway 管理（db/migration/V*.sql），此处仅作备用占位
-- =============================================================================

-- 确保数据库使用 utf8mb4
ALTER DATABASE library_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
